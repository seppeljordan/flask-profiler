#!/usr/bin/env python

import subprocess


def main():
    subprocess.run(["black", "."])
    subprocess.run(["isort", "."])
    nix_fmt()


def nix_fmt():
    subprocess.run(["nix", "fmt"])


if __name__ == "__main__":
    main()
